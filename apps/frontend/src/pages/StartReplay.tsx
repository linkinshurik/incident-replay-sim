import React from 'react';

const StartReplay: React.FC = () => {
  return (
    <div>
      <h1>Start Replay</h1>
      <p>Basic page skeleton for starting a replay.</p>
    </div>
  );
};

export default StartReplay;
