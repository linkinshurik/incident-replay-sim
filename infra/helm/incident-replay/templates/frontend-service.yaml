apiVersion: v1
kind: Service
metadata:
  name: {{ include "incident-replay.frontend.fullname" . }}
  labels:
    {{- include "incident-replay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: {{ include "incident-replay.frontend.fullname" . }}
